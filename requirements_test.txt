pytest
# Pinned: this version pins homeassistant==2023.1.7, the HA the suite is
# verified green against. An unpinned range lets CI pull a newer HA whose
# entity/selector behaviour differs and silently reintroduces failures.
pytest-homeassistant-custom-component==0.12.49
pytest-asyncio
pytest-cov
# Required to set up the `http` dependency declared in manifest.json during
# config-flow tests (HA's http component imports aiohttp_cors). Absent from the
# base test package, so CI fails with "No module named 'aiohttp_cors'" without it.
aiohttp-cors
